########################################################
# Started Logging At: 2025-12-09 09:28:01
########################################################
########################################################
# # Started Logging At: 2025-12-09 09:28:01
########################################################
import sys as _VSCODE_sys
print(_VSCODE_sys.executable); del _VSCODE_sys
try:
    import ipywidgets as _VSCODE_ipywidgets
    print("e976ee50-99ed-4aba-9b6b-9dcd5634d07d:IPyWidgets:" + _VSCODE_ipywidgets.__version__)
    del _VSCODE_ipywidgets
except:
    pass
def __VSCODE_inject_module():

    def __VSCODE_call_function(function, callback, data=None):
        __VSCODE_send_chat_message__(function, data, callback=callback)

    def __VSCODE_send_chat_message__(function, data, callback):
        requests = {}
        try:
            requests = __VSCODE_send_chat_message__.__requests
        except Exception:
            __VSCODE_send_chat_message__.__requests = requests

        import uuid as __VSCODE_send_chat_message__uuid
        import IPython.display as __VSCODE_send_chat_message__ipython_display

        id = str(__VSCODE_send_chat_message__uuid.uuid4())
        requests[id] = callback
        data_is_none = data is None
        __VSCODE_send_chat_message__ipython_display.display({"application/vnd.vscode.chat_message": data}, metadata={"id":id, "function": function, "dataIsNone": data_is_none}, raw=True)

        del __VSCODE_send_chat_message__ipython_display
        del __VSCODE_send_chat_message__uuid

    def __VSCODE_on_chat_message(id, data):
        requests = {}
        try:
            requests = __VSCODE_send_chat_message__.__requests
        except Exception:
            __VSCODE_send_chat_message__.__requests = requests

        if id in requests:
            requests[id](data)
            del requests[id]
        else:
            raise NotImplementedError(f"Callback not found for message {id}")

    import sys as __VSCODE_send_chat_message__sys
    import IPython as __VSCODE_send_chat_message__IPython
    chat = type(__VSCODE_send_chat_message__IPython)("chat")
    chat.send_message = __VSCODE_send_chat_message__
    chat.call_function = __VSCODE_call_function
    chat.__on_message = __VSCODE_on_chat_message
    __VSCODE_send_chat_message__sys.modules["vscode"] = type(__VSCODE_send_chat_message__IPython)("vscode")
    __VSCODE_send_chat_message__sys.modules["vscode"].chat = chat
    del __VSCODE_send_chat_message__sys
    del __VSCODE_send_chat_message__IPython


__VSCODE_inject_module()
del __VSCODE_inject_module

__vsc_ipynb_file__ = "/blue/adamginsburg/t.yoo/from_red/w51/w51_catalog/notebooks/check_all_catalog.ipynb"
get_ipython().run_line_magic('config', 'Completer.use_jedi = False')
import glob
from astropy.table import Table
from astropy.io import fits
from astropy.wcs import WCS
from astropy.coordinates import SkyCoord
import matplotlib.pyplot as plt
from astropy.visualization import simple_norm
import numpy as np
from jwst.datamodels import dqflags
from scipy.ndimage import label, find_objects, center_of_mass, sum_labels


def main(xpos, ypos, size=100, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' ):
    #jw06151001001_03109_00007_nrcblong_cal.fits
    imgdir = f'/orange/adamginsburg/jwst/w51/{filter.upper()}/'
    img_filename = imgdir + '/pipeline/'+f'jw06151001001_0{vgroup}_{expnum}_{module}_cal.fits'
    img = fits.open(img_filename)

    fig = plt.figure(figsize=(30,15))
    ax1 = fig.add_subplot(1,2,1)
    ax2 = fig.add_subplot(1,2,2)
    wcs_img = WCS(img[1].header)
    norm = simple_norm(img[1].data, 'asinh', min_percent=1, max_percent=99)
    ax1.imshow(img[1].data, norm=norm, origin='lower', cmap='inferno')
    square = plt.Rectangle((xpos - size/2, ypos - size/2), size, size, edgecolor='cyan', facecolor='none', lw=2)
    ax1.add_patch(square)
    ax1.set_title(f'Image: {img_filename}', fontsize=16)
    # f480m_nrcb_visit001_vgroup3109_exp00007_daophot_basic.fits
    wav = int(filter.lower()[1:4])
    if wav > 300:
        module2 = module[:-4]  #nrca or nrcb
    else:
        module2 = module
    cat_basic_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_basic.fits'
    cat_refined_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_refined.fits'

    #jw06151001001_03109_00007_nrcblong_align_o001_crf_satstar_catalog_newnewnewnew.fits
    sat_cat_filename = imgdir + '/pipeline/' f'jw06151001001_0{vgroup}_{expnum}_{module}_align_o001_crf_satstar_catalog_newnewnewnew.fits'

    cutout_xmin = int(xpos - size/2)
    cutout_xmax = int(xpos + size/2)
    cutout_ymin = int(ypos - size/2)
    cutout_ymax = int(ypos + size/2)
    cutout = img[1].data[cutout_ymin:cutout_ymax, cutout_xmin:cutout_xmax]
    norm = simple_norm(cutout, 'asinh', min_percent=1, max_percent=99)

    ax2.imshow(cutout, norm=norm, origin='lower', cmap='inferno')
    ax2.set_title(f'Cutout centered at ({xpos}, {ypos})', fontsize=16)
    
    cat_basic = Table.read(cat_basic_filename)
    cat_refined = Table.read(cat_refined_filename)
    sat_cat = Table.read(sat_cat_filename)

    xpos_basic = cat_basic['x_fit']
    ypos_basic = cat_basic['y_fit']
    xpos_refined = cat_refined['x_fit']
    ypos_refined = cat_refined['y_fit']
    print(sat_cat.colnames)

    xpos_sat = sat_cat['xcentroid']
    ypos_sat = sat_cat['ycentroid']
    ax1.scatter(xpos_sat, ypos_sat, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)

    ax2.scatter(xpos_basic-cutout_xmin, ypos_basic-cutout_ymin, s=500, edgecolor='blue', facecolor='none', label='Basic Catalog', lw=2)
    ax2.scatter(xpos_refined-cutout_xmin, ypos_refined-cutout_ymin, s=500, edgecolor='green', facecolor='none', label='Refined Catalog', lw=2)
    ax2.scatter(xpos_sat-cutout_xmin, ypos_sat-cutout_ymin, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)
    ax2.set_xlim(0, size)
    ax2.set_ylim(0, size)

    dqarr = fits.open(img_filename)['DQ'].data
    is_saturated = (dqarr & dqflags.pixel['SATURATED']) != 0
    labeled_array, num_features = label(is_saturated)
    centers = center_of_mass(is_saturated, labeled_array, range(1, num_features + 1))
    for center in centers:
        ycen, xcen = center
        if cutout_xmin <= xcen < cutout_xmax and cutout_ymin <= ycen < cutout_ymax:
            ax2.scatter(xcen - cutout_xmin, ycen - cutout_ymin, s=800, edgecolor='magenta', facecolor='none', label='Saturated Star Center', lw=2)
    return img['SCI'].data, img['ERR'].data, cutout
data, err, cutout = main(480, 2000, size=20, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' )   


from photutils.detection import DAOStarFinder
from astropy import stats
basepath = '/orange/adamginsburg/jwst/w51/'
filtered_errest = np.nanmedian(err)

fwhm_tbl = Table.read(f'{basepath}/reduction/fwhm_table.ecsv')

filtername = 'F480M'

row = fwhm_tbl[fwhm_tbl['Filter'] == filtername]
fwhm = fwhm_arcsec = float(row['PSF FWHM (arcsec)'][0])
fwhm_pix = float(row['PSF FWHM (pixel)'][0])
print(filtered_errest)

daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix,# roundhi=1.0, roundlo=-1.0,
                                  #sharplo=0.30, sharphi=1.40)
                                  )


finstars = daofind_tuned(np.nan_to_num(cutout))
finstars.pprint(max_lines=-1)
import glob
from astropy.table import Table
from astropy.io import fits
from astropy.wcs import WCS
from astropy.coordinates import SkyCoord
import matplotlib.pyplot as plt
from astropy.visualization import simple_norm
import numpy as np
from jwst.datamodels import dqflags
from scipy.ndimage import label, find_objects, center_of_mass, sum_labels


def main(xpos, ypos, size=100, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' ):
    #jw06151001001_03109_00007_nrcblong_cal.fits
    imgdir = f'/orange/adamginsburg/jwst/w51/{filter.upper()}/'
    img_filename = imgdir + '/pipeline/'+f'jw06151001001_0{vgroup}_{expnum}_{module}_cal.fits'
    img = fits.open(img_filename)

    fig = plt.figure(figsize=(30,15))
    ax1 = fig.add_subplot(1,2,1)
    ax2 = fig.add_subplot(1,2,2)
    wcs_img = WCS(img[1].header)
    norm = simple_norm(img[1].data, 'asinh', min_percent=1, max_percent=99)
    ax1.imshow(img[1].data, norm=norm, origin='lower', cmap='inferno')
    square = plt.Rectangle((xpos - size/2, ypos - size/2), size, size, edgecolor='cyan', facecolor='none', lw=2)
    ax1.add_patch(square)
    ax1.set_title(f'Image: {img_filename}', fontsize=16)
    # f480m_nrcb_visit001_vgroup3109_exp00007_daophot_basic.fits
    wav = int(filter.lower()[1:4])
    if wav > 300:
        module2 = module[:-4]  #nrca or nrcb
    else:
        module2 = module
    cat_basic_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_basic.fits'
    cat_refined_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_refined.fits'

    #jw06151001001_03109_00007_nrcblong_align_o001_crf_satstar_catalog_newnewnewnew.fits
    sat_cat_filename = imgdir + '/pipeline/' f'jw06151001001_0{vgroup}_{expnum}_{module}_align_o001_crf_satstar_catalog_newnewnewnew.fits'

    cutout_xmin = int(xpos - size/2)
    cutout_xmax = int(xpos + size/2)
    cutout_ymin = int(ypos - size/2)
    cutout_ymax = int(ypos + size/2)
    cutout = img[1].data[cutout_ymin:cutout_ymax, cutout_xmin:cutout_xmax]
    norm = simple_norm(cutout, 'asinh', min_percent=1, max_percent=99)

    ax2.imshow(cutout, norm=norm, origin='lower', cmap='inferno')
    ax2.set_title(f'Cutout centered at ({xpos}, {ypos})', fontsize=16)
    
    cat_basic = Table.read(cat_basic_filename)
    cat_refined = Table.read(cat_refined_filename)
    sat_cat = Table.read(sat_cat_filename)

    xpos_basic = cat_basic['x_fit']
    ypos_basic = cat_basic['y_fit']
    xpos_refined = cat_refined['x_fit']
    ypos_refined = cat_refined['y_fit']
    print(sat_cat.colnames)

    xpos_sat = sat_cat['xcentroid']
    ypos_sat = sat_cat['ycentroid']
    ax1.scatter(xpos_sat, ypos_sat, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)

    ax2.scatter(xpos_basic-cutout_xmin, ypos_basic-cutout_ymin, s=500, edgecolor='blue', facecolor='none', label='Basic Catalog', lw=2)
    ax2.scatter(xpos_refined-cutout_xmin, ypos_refined-cutout_ymin, s=500, edgecolor='green', facecolor='none', label='Refined Catalog', lw=2)
    ax2.scatter(xpos_sat-cutout_xmin, ypos_sat-cutout_ymin, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)
    ax2.set_xlim(0, size)
    ax2.set_ylim(0, size)

    dqarr = fits.open(img_filename)['DQ'].data
    is_saturated = (dqarr & dqflags.pixel['SATURATED']) != 0
    labeled_array, num_features = label(is_saturated)
    centers = center_of_mass(is_saturated, labeled_array, range(1, num_features + 1))
    for center in centers:
        ycen, xcen = center
        if cutout_xmin <= xcen < cutout_xmax and cutout_ymin <= ycen < cutout_ymax:
            ax2.scatter(xcen - cutout_xmin, ycen - cutout_ymin, s=800, edgecolor='magenta', facecolor='none', label='Saturated Star Center', lw=2)
    return img['SCI'].data, img['ERR'].data, cutout
data, err, cutout = main(480, 2000, size=50, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' )   


import glob
from astropy.table import Table
from astropy.io import fits
from astropy.wcs import WCS
from astropy.coordinates import SkyCoord
import matplotlib.pyplot as plt
from astropy.visualization import simple_norm
import numpy as np
from jwst.datamodels import dqflags
from scipy.ndimage import label, find_objects, center_of_mass, sum_labels


def main(xpos, ypos, size=100, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' ):
    #jw06151001001_03109_00007_nrcblong_cal.fits
    imgdir = f'/orange/adamginsburg/jwst/w51/{filter.upper()}/'
    img_filename = imgdir + '/pipeline/'+f'jw06151001001_0{vgroup}_{expnum}_{module}_cal.fits'
    img = fits.open(img_filename)

    fig = plt.figure(figsize=(30,15))
    ax1 = fig.add_subplot(1,2,1)
    ax2 = fig.add_subplot(1,2,2)
    wcs_img = WCS(img[1].header)
    norm = simple_norm(img[1].data, 'asinh', min_percent=1, max_percent=99)
    ax1.imshow(img[1].data, norm=norm, origin='lower', cmap='inferno')
    square = plt.Rectangle((xpos - size/2, ypos - size/2), size, size, edgecolor='cyan', facecolor='none', lw=2)
    ax1.add_patch(square)
    ax1.set_title(f'Image: {img_filename}', fontsize=16)
    # f480m_nrcb_visit001_vgroup3109_exp00007_daophot_basic.fits
    wav = int(filter.lower()[1:4])
    if wav > 300:
        module2 = module[:-4]  #nrca or nrcb
    else:
        module2 = module
    cat_basic_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_basic.fits'
    cat_refined_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_refined.fits'

    #jw06151001001_03109_00007_nrcblong_align_o001_crf_satstar_catalog_newnewnewnew.fits
    sat_cat_filename = imgdir + '/pipeline/' f'jw06151001001_0{vgroup}_{expnum}_{module}_align_o001_crf_satstar_catalog_newnewnewnew.fits'

    cutout_xmin = int(xpos - size/2)
    cutout_xmax = int(xpos + size/2)
    cutout_ymin = int(ypos - size/2)
    cutout_ymax = int(ypos + size/2)
    cutout = img[1].data[cutout_ymin:cutout_ymax, cutout_xmin:cutout_xmax]
    norm = simple_norm(cutout, 'asinh', min_percent=1, max_percent=99)

    ax2.imshow(cutout, norm=norm, origin='lower', cmap='inferno')
    ax2.set_title(f'Cutout centered at ({xpos}, {ypos})', fontsize=16)
    
    cat_basic = Table.read(cat_basic_filename)
    cat_refined = Table.read(cat_refined_filename)
    sat_cat = Table.read(sat_cat_filename)

    xpos_basic = cat_basic['x_fit']
    ypos_basic = cat_basic['y_fit']
    xpos_refined = cat_refined['x_fit']
    ypos_refined = cat_refined['y_fit']
    print(sat_cat.colnames)

    xpos_sat = sat_cat['xcentroid']
    ypos_sat = sat_cat['ycentroid']
    ax1.scatter(xpos_sat, ypos_sat, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)

    ax2.scatter(xpos_basic-cutout_xmin, ypos_basic-cutout_ymin, s=500, edgecolor='blue', facecolor='none', label='Basic Catalog', lw=2)
    ax2.scatter(xpos_refined-cutout_xmin, ypos_refined-cutout_ymin, s=500, edgecolor='green', facecolor='none', label='Refined Catalog', lw=2)
    ax2.scatter(xpos_sat-cutout_xmin, ypos_sat-cutout_ymin, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)
    ax2.set_xlim(0, size)
    ax2.set_ylim(0, size)

    dqarr = fits.open(img_filename)['DQ'].data
    is_saturated = (dqarr & dqflags.pixel['SATURATED']) != 0
    labeled_array, num_features = label(is_saturated)
    centers = center_of_mass(is_saturated, labeled_array, range(1, num_features + 1))
    for center in centers:
        ycen, xcen = center
        if cutout_xmin <= xcen < cutout_xmax and cutout_ymin <= ycen < cutout_ymax:
            ax2.scatter(xcen - cutout_xmin, ycen - cutout_ymin, s=800, edgecolor='magenta', facecolor='none', label='Saturated Star Center', lw=2)
    return img['SCI'].data, img['ERR'].data, cutout
data, err, cutout = main(455, 2000, size=50, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' )   


from photutils.detection import DAOStarFinder
from astropy import stats
basepath = '/orange/adamginsburg/jwst/w51/'
filtered_errest = np.nanmedian(err)

fwhm_tbl = Table.read(f'{basepath}/reduction/fwhm_table.ecsv')

filtername = 'F480M'

row = fwhm_tbl[fwhm_tbl['Filter'] == filtername]
fwhm = fwhm_arcsec = float(row['PSF FWHM (arcsec)'][0])
fwhm_pix = float(row['PSF FWHM (pixel)'][0])
print(filtered_errest)

daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix,# roundhi=1.0, roundlo=-1.0,
                                  #sharplo=0.30, sharphi=1.40)
                                  )


finstars = daofind_tuned(np.nan_to_num(cutout))
finstars.pprint(max_lines=-1)
import glob
from astropy.table import Table
from astropy.io import fits
from astropy.wcs import WCS
from astropy.coordinates import SkyCoord
import matplotlib.pyplot as plt
from astropy.visualization import simple_norm
import numpy as np
from jwst.datamodels import dqflags
from scipy.ndimage import label, find_objects, center_of_mass, sum_labels


def main(xpos, ypos, size=100, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' ):
    #jw06151001001_03109_00007_nrcblong_cal.fits
    imgdir = f'/orange/adamginsburg/jwst/w51/{filter.upper()}/'
    img_filename = imgdir + '/pipeline/'+f'jw06151001001_0{vgroup}_{expnum}_{module}_cal.fits'
    img = fits.open(img_filename)

    fig = plt.figure(figsize=(30,15))
    ax1 = fig.add_subplot(1,2,1)
    ax2 = fig.add_subplot(1,2,2)
    wcs_img = WCS(img[1].header)
    norm = simple_norm(img[1].data, 'asinh', min_percent=1, max_percent=99)
    ax1.imshow(img[1].data, norm=norm, origin='lower', cmap='inferno')
    square = plt.Rectangle((xpos - size/2, ypos - size/2), size, size, edgecolor='cyan', facecolor='none', lw=2)
    ax1.add_patch(square)
    ax1.set_title(f'Image: {img_filename}', fontsize=16)
    # f480m_nrcb_visit001_vgroup3109_exp00007_daophot_basic.fits
    wav = int(filter.lower()[1:4])
    if wav > 300:
        module2 = module[:-4]  #nrca or nrcb
    else:
        module2 = module
    cat_basic_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_basic.fits'
    cat_refined_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_refined.fits'

    #jw06151001001_03109_00007_nrcblong_align_o001_crf_satstar_catalog_newnewnewnew.fits
    sat_cat_filename = imgdir + '/pipeline/' f'jw06151001001_0{vgroup}_{expnum}_{module}_align_o001_crf_satstar_catalog_newnewnewnew.fits'

    cutout_xmin = int(xpos - size/2)
    cutout_xmax = int(xpos + size/2)
    cutout_ymin = int(ypos - size/2)
    cutout_ymax = int(ypos + size/2)
    cutout = img[1].data[cutout_ymin:cutout_ymax, cutout_xmin:cutout_xmax]
    norm = simple_norm(cutout, 'asinh', min_percent=1, max_percent=99)

    ax2.imshow(cutout, norm=norm, origin='lower', cmap='inferno')
    ax2.set_title(f'Cutout centered at ({xpos}, {ypos})', fontsize=16)
    
    cat_basic = Table.read(cat_basic_filename)
    cat_refined = Table.read(cat_refined_filename)
    sat_cat = Table.read(sat_cat_filename)

    xpos_basic = cat_basic['x_fit']
    ypos_basic = cat_basic['y_fit']
    xpos_refined = cat_refined['x_fit']
    ypos_refined = cat_refined['y_fit']
    print(sat_cat.colnames)

    xpos_sat = sat_cat['xcentroid']
    ypos_sat = sat_cat['ycentroid']
    ax1.scatter(xpos_sat, ypos_sat, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)

    ax2.scatter(xpos_basic-cutout_xmin, ypos_basic-cutout_ymin, s=500, edgecolor='blue', facecolor='none', label='Basic Catalog', lw=2)
    ax2.scatter(xpos_refined-cutout_xmin, ypos_refined-cutout_ymin, s=500, edgecolor='green', facecolor='none', label='Refined Catalog', lw=2)
    ax2.scatter(xpos_sat-cutout_xmin, ypos_sat-cutout_ymin, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)
    ax2.set_xlim(0, size)
    ax2.set_ylim(0, size)

    dqarr = fits.open(img_filename)['DQ'].data
    is_saturated = (dqarr & dqflags.pixel['SATURATED']) != 0
    labeled_array, num_features = label(is_saturated)
    centers = center_of_mass(is_saturated, labeled_array, range(1, num_features + 1))
    for center in centers:
        ycen, xcen = center
        if cutout_xmin <= xcen < cutout_xmax and cutout_ymin <= ycen < cutout_ymax:
            ax2.scatter(xcen - cutout_xmin, ycen - cutout_ymin, s=800, edgecolor='magenta', facecolor='none', label='Saturated Star Center', lw=2)
    return img['SCI'].data, img['ERR'].data, cutout
data, err, cutout = main(450, 2000, size=50, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' )   


from photutils.detection import DAOStarFinder
from astropy import stats
basepath = '/orange/adamginsburg/jwst/w51/'
filtered_errest = np.nanmedian(err)

fwhm_tbl = Table.read(f'{basepath}/reduction/fwhm_table.ecsv')

filtername = 'F480M'

row = fwhm_tbl[fwhm_tbl['Filter'] == filtername]
fwhm = fwhm_arcsec = float(row['PSF FWHM (arcsec)'][0])
fwhm_pix = float(row['PSF FWHM (pixel)'][0])
print(filtered_errest)

daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix,# roundhi=1.0, roundlo=-1.0,
                                  #sharplo=0.30, sharphi=1.40)
                                  )


finstars = daofind_tuned(np.nan_to_num(cutout))
finstars.pprint(max_lines=-1)
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.0": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("cu", 2)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.0.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.0.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("cutout", 6, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
cutout[28:38, 30:40]
#[Out]# array([[30.964502, 32.07793 , 31.690294, 31.534588, 32.212914, 32.7543  ,
#[Out]#         32.173683, 32.727886, 31.668354, 32.239162],
#[Out]#        [31.059711, 31.558249, 31.093742, 31.249773, 32.93707 , 33.14296 ,
#[Out]#         32.81087 , 32.932922, 33.31726 , 32.379574],
#[Out]#        [30.949366, 31.79206 , 31.476746, 31.357334, 33.689957, 37.110947,
#[Out]#         35.78524 , 33.398914, 33.09975 , 32.7724  ],
#[Out]#        [30.621435, 32.21477 , 32.205322,       nan, 37.998882, 41.522045,
#[Out]#         38.642937, 34.92624 , 32.942623, 33.111748],
#[Out]#        [31.585247, 30.827196, 30.6575  , 32.793053, 36.152946, 40.279076,
#[Out]#         38.89247 , 34.438087, 33.09562 , 33.576077],
#[Out]#        [31.371939, 31.61736 , 31.643291, 31.895227, 33.49506 , 34.847324,
#[Out]#         35.33985 , 32.82683 , 32.943775, 32.654346],
#[Out]#        [30.238377, 31.43596 , 30.916313, 31.385044, 31.088085, 31.70609 ,
#[Out]#         31.539515, 32.015682, 32.974174, 33.61422 ],
#[Out]#        [31.178288, 30.914255, 30.29068 , 30.532543, 30.808067, 31.755266,
#[Out]#         31.501379, 31.65471 , 32.356205, 32.338024],
#[Out]#        [30.728382, 31.253284, 30.996904, 30.952894, 31.587542, 31.811686,
#[Out]#         31.30703 , 31.744236, 32.786343, 32.542862],
#[Out]#        [32.077297, 31.74161 , 31.369831, 31.635403, 30.920923, 31.90437 ,
#[Out]#         32.000687, 33.29016 , 32.24078 , 32.22452 ]], dtype='>f4')
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.2": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plcutout[28:38, 30:40]", 1)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.2.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.2.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.3": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imcutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.3.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.3.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.4": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imscutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.4.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.4.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.5": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshocutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.5.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.5.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.6": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshocutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.6.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.6.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.7": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshocutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.7.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.7.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.8": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshocutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.8.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.8.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.9": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshocutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.9.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.9.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.10": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshowcutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.10.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.10.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.11": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshowcutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.11.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.11.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.12": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshowcutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.12.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.12.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.13": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshowcutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.13.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.13.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.14": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.14.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.14.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.15": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.15.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.15.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.16": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.16.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.16.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.17": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.17.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.17.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.18": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.18.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.18.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.19": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40]", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.19.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.19.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.20": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40])", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.20.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.20.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.21": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40])", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.21.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.21.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
pl.imshow(cutout[28:38, 30:40])
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.22": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("pl.imshow(cutout[28:38, 30:40])", 4)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.22.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.22.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.23": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])", 3)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.23.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.23.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:38, 30:40])
#[Out]# <matplotlib.image.AxesImage at 0x153b80136630>
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.24": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nda", 34)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.24.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.24.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.25": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("delattr", 7, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.25.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.25.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.26": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndao", 36)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.26.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.26.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.27": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned(c", 48)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.27.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.27.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.28": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned(cutou", 51)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.28.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.28.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:38, 30:40])
daofind_tuned(cutout[28:38, 30:40])
#[Out]# <QTable length=3>
#[Out]#   id      xcentroid          ycentroid     ...    mag         daofind_mag    
#[Out]# int64      float64            float64      ...  float32         float64      
#[Out]# ----- ------------------ ----------------- ... ---------- -------------------
#[Out]#     1  7.572234476670005 1.449504502852097 ...  -6.820741 -3.3320787401584235
#[Out]#     2 1.3530137213281472 7.671848674736141 ... -6.7423034  -3.273273054671958
#[Out]#     3  7.670137051638599 7.638533644273333 ... -6.7818985 -3.3304077290602105
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.29": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfdaofind_tuned(cutout[28:38, 30:40])", 34)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.29.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.29.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.30": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\n\nf", 77)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.30.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.30.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.31": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("finally", 7, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.31.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.31.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.32": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\npl\nfin", 76)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.32.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.32.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.33": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\npl.\nfin", 78)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.33.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.33.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.34": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\npl.\nfin", 78)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.34.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.34.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.35": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\npl.\nfin", 78)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.35.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.35.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.36": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\npl\nfin", 77)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.36.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.36.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.37": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\nplt.\nfin", 79)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.37.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.37.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.38": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.ScalarFormatter", 19, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.38.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.38.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.39": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\nplt.scatter(f\nfin", 88)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.39.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.39.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.40": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cfinally", 19, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.40.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.40.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.41": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\nfin = daofind_tuned(cutout[28:38, 30:40])\nplt.scatter(fin[''\nfin", 92)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.41.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.41.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.42": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cutoutalpha=", 23, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.42.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.42.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.43": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cutoutx=", 19, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.43.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.43.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:38, 30:40])
fin = daofind_tuned(cutout[28:38, 30:40])
plt.scatter(fin['xcentroid'], fin['ycentroid'], s=5, edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=3>
#[Out]#   id      xcentroid          ycentroid     ...    mag         daofind_mag    
#[Out]# int64      float64            float64      ...  float32         float64      
#[Out]# ----- ------------------ ----------------- ... ---------- -------------------
#[Out]#     1  7.572234476670005 1.449504502852097 ...  -6.820741 -3.3320787401584235
#[Out]#     2 1.3530137213281472 7.671848674736141 ... -6.7423034  -3.273273054671958
#[Out]#     3  7.670137051638599 7.638533644273333 ... -6.7818985 -3.3304077290602105
plt.imshow(cutout[28:38, 30:40])
fin = daofind_tuned(cutout[28:38, 30:40])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=3>
#[Out]#   id      xcentroid          ycentroid     ...    mag         daofind_mag    
#[Out]# int64      float64            float64      ...  float32         float64      
#[Out]# ----- ------------------ ----------------- ... ---------- -------------------
#[Out]#     1  7.572234476670005 1.449504502852097 ...  -6.820741 -3.3320787401584235
#[Out]#     2 1.3530137213281472 7.671848674736141 ... -6.7423034  -3.273273054671958
#[Out]#     3  7.670137051638599 7.638533644273333 ... -6.7818985 -3.3304077290602105
plt.imshow(cutout[28:38, 30:40])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)
                                  )

fin = daofind_tuned(cutout[28:38, 30:40])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
plt.imshow(cutout[28:38, 30:40])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned(cutout[28:38, 30:40])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=4>
#[Out]#   id      xcentroid          ycentroid     ...    mag         daofind_mag    
#[Out]# int64      float64            float64      ...  float32         float64      
#[Out]# ----- ------------------ ----------------- ... ---------- -------------------
#[Out]#     1  7.572234476670005 1.449504502852097 ...  -6.820741 -3.3320787401584235
#[Out]#     2 1.3530137213281472 7.671848674736141 ... -6.7423034  -3.273273054671958
#[Out]#     3  7.071918382356742 7.652757095374165 ... -7.0003166 -2.5731895184499436
#[Out]#     4  7.670137051638599 7.638533644273333 ... -6.7818985 -3.3304077290602105
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.44": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 34)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.44.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.44.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
cutout[28:38, 30:40]
#[Out]# array([[30.964502, 32.07793 , 31.690294, 31.534588, 32.212914, 32.7543  ,
#[Out]#         32.173683, 32.727886, 31.668354, 32.239162],
#[Out]#        [31.059711, 31.558249, 31.093742, 31.249773, 32.93707 , 33.14296 ,
#[Out]#         32.81087 , 32.932922, 33.31726 , 32.379574],
#[Out]#        [30.949366, 31.79206 , 31.476746, 31.357334, 33.689957, 37.110947,
#[Out]#         35.78524 , 33.398914, 33.09975 , 32.7724  ],
#[Out]#        [30.621435, 32.21477 , 32.205322,       nan, 37.998882, 41.522045,
#[Out]#         38.642937, 34.92624 , 32.942623, 33.111748],
#[Out]#        [31.585247, 30.827196, 30.6575  , 32.793053, 36.152946, 40.279076,
#[Out]#         38.89247 , 34.438087, 33.09562 , 33.576077],
#[Out]#        [31.371939, 31.61736 , 31.643291, 31.895227, 33.49506 , 34.847324,
#[Out]#         35.33985 , 32.82683 , 32.943775, 32.654346],
#[Out]#        [30.238377, 31.43596 , 30.916313, 31.385044, 31.088085, 31.70609 ,
#[Out]#         31.539515, 32.015682, 32.974174, 33.61422 ],
#[Out]#        [31.178288, 30.914255, 30.29068 , 30.532543, 30.808067, 31.755266,
#[Out]#         31.501379, 31.65471 , 32.356205, 32.338024],
#[Out]#        [30.728382, 31.253284, 30.996904, 30.952894, 31.587542, 31.811686,
#[Out]#         31.30703 , 31.744236, 32.786343, 32.542862],
#[Out]#        [32.077297, 31.74161 , 31.369831, 31.635403, 30.920923, 31.90437 ,
#[Out]#         32.000687, 33.29016 , 32.24078 , 32.22452 ]], dtype='>f4')
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.45": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(ncutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 254)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.45.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.45.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.46": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.46.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.46.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.47": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.47.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.47.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.48": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.ncutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.48.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.48.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.49": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nacutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.49.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.49.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.50": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nacutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.50.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.50.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.51": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nancutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.51.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.51.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.52": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.52.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.52.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.53": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_tcutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.53.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.53.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.54": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_tocutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.54.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.54.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.55": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_tocutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.55.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.55.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.56": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_tocutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.56.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.56.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.57": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.57.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.57.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.58": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.58.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.58.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.59": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_ncutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.59.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.59.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.60": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_nucutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.60.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.60.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.61": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_nucutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.61.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.61.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.62": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_numcutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.62.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.62.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.63": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_numcutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.63.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.63.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.64": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.64.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.64.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.65": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.65.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.65.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.66": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.66.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.66.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.67": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.67.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.67.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.68": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.68.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.68.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.69": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.69.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.69.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.70": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.70.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.70.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.71": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.71.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.71.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.72": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.72.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.72.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.73": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.73.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.73.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:38, 30:40])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=5>
#[Out]#   id      xcentroid          ycentroid     ...    mag         daofind_mag    
#[Out]# int64      float64            float64      ...  float32         float64      
#[Out]# ----- ------------------ ----------------- ... ---------- -------------------
#[Out]#     1  7.572234476670005 1.449504502852097 ...  -6.820741 -3.3320787401584235
#[Out]#     2  5.298922426521624 4.523890585990861 ...  -7.308714  -3.197333707901482
#[Out]#     3 1.3530137213281472 7.671848674736141 ... -6.7423034  -3.273273054671958
#[Out]#     4  7.071918382356742 7.652757095374165 ... -7.0003166 -2.5731895184499436
#[Out]#     5  7.670137051638599 7.638533644273333 ... -6.7818985 -3.3304077290602105
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.74": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.74.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.74.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.75": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.75.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.75.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.76": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.76.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.76.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.77": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.77.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.77.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.78": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.78.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.78.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.79": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.79.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.79.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.80": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.80.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.80.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.81": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.81.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.81.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.82": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.82.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.82.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.83": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.83.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.83.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.84": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.84.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.84.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.85": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.85.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.85.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.86": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.86.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.86.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.87": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.87.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.87.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.88": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.88.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.88.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.89": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.89.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.89.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.90": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.90.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.90.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.91": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.91.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.91.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.92": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.92.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.92.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.93": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.93.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.93.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.94": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.94.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.94.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.95": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.95.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.95.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.96": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.96.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.96.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.97": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.97.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.97.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.98": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.98.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.98.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.99": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.99.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.99.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.100": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.100.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.100.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.101": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.101.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.101.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.102": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.102.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.102.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.103": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.103.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.103.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.104": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.104.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.104.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.105": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.105.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.105.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.106": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.106.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.106.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.107": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.107.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.107.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.108": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.108.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.108.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.109": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.109.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.109.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.110": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.110.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.110.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.111": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.111.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.111.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.112": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.112.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.112.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.113": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.113.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.113.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.114": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.114.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.114.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.115": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.115.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.115.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.116": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.116.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.116.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.117": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.117.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.117.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.118": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.118.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.118.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.119": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.119.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.119.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.120": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.120.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.120.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.121": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.121.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.121.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.122": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.122.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.122.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.123": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.123.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.123.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.124": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.124.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.124.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.125": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.125.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.125.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.126": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.126.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.126.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.127": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.127.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.127.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.128": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.128.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.128.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.129": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.129.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.129.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.130": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.130.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.130.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.131": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.131.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.131.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.132": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.132.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.132.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.133": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.133.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.133.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.134": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.134.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.134.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.135": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.135.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.135.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.136": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.136.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.136.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.137": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.137.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.137.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.138": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.138.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.138.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.139": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.139.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.139.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.140": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.140.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.140.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.141": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.141.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.141.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.142": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.142.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.142.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.143": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.143.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.143.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.144": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.144.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.144.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.145": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.145.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.145.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.146": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.146.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.146.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.147": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.147.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.147.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.148": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.148.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.148.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.149": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.149.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.149.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.150": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.150.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.150.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.151": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.151.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.151.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.152": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.152.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.152.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.153": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.153.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.153.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.154": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.154.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.154.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.155": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.155.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.155.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.156": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.156.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.156.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.157": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.157.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.157.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.158": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.158.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.158.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.159": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.159.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.159.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.160": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.160.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.160.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.161": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.161.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.161.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.162": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.162.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.162.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.163": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.163.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.163.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.164": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.164.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.164.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.165": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.165.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.165.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.166": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.166.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.166.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.167": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.167.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.167.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.168": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.168.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.168.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.169": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.169.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.169.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.170": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.170.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.170.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.171": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.171.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.171.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.172": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.172.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.172.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.173": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.173.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.173.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.174": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.174.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.174.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.175": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.175.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.175.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.176": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.176.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.176.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.177": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.177.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.177.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.178": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.178.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.178.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.179": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.179.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.179.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.180": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.180.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.180.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.181": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.181.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.181.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.182": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.182.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.182.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.183": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.183.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.183.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.184": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.184.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.184.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.185": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.185.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.185.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.186": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.186.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.186.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.187": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.187.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.187.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.188": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.188.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.188.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.189": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.189.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.189.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.190": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:38, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.190.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.190.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.191": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.191.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.191.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.192": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.192.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.192.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.193": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:4, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.193.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.193.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.194": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:4, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.194.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.194.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.195": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.195.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.195.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.196": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.196.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.196.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.197": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.197.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.197.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.198": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.198.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.198.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.199": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.199.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.199.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.200": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.200.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.200.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.201": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.201.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.201.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.202": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.202.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.202.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.203": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.203.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.203.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.204": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.204.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.204.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.205": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.205.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.205.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.206": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.206.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.206.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.207": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.207.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.207.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.208": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.208.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.208.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.209": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:40]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.209.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.209.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.210": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.210.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.210.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.211": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.211.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.211.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.212": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.212.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.212.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.213": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.213.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.213.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.214": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.214.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.214.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.215": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.215.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.215.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.216": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.216.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.216.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.217": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.217.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.217.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.218": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.218.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.218.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.219": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.219.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.219.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.220": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.220.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.220.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.221": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.221.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.221.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.222": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.222.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.222.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.223": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.223.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.223.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.224": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.224.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.224.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.225": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.225.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.225.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.226": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.226.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.226.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.227": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.227.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.227.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.228": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.228.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.228.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.229": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.229.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.229.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.230": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.230.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.230.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.231": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.231.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.231.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.232": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.232.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.232.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.233": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.233.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.233.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.234": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.234.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.234.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.235": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.235.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.235.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.236": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.236.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.236.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.237": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.237.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.237.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.238": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.238.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.238.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.239": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.239.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.239.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.240": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:40])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.240.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.240.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.241": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.241.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.241.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.242": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.242.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.242.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.243": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.243.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.243.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.244": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.244.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.244.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.245": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.245.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.245.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.246": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.246.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.246.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.247": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.247.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.247.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.248": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.248.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.248.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.249": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.249.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.249.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.250": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.250.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.250.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.251": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.251.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.251.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.252": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.252.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.252.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.253": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.253.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.253.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.254": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.254.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.254.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.255": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.255.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.255.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.256": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:38, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.256.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.256.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.257": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 254)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.257.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.257.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.258": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 254)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.258.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.258.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.259": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:4, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 255)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.259.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.259.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.260": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.260.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.260.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.261": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.261.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.261.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.262": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.262.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.262.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.263": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.263.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.263.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.264": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.264.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.264.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.265": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.265.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.265.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.266": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.266.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.266.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.267": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.267.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.267.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.268": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.268.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.268.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.269": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.269.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.269.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.270": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.270.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.270.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.271": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.271.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.271.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.272": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.272.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.272.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.273": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.273.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.273.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.274": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.274.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.274.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.275": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.275.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.275.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.276": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.276.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.276.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.277": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.277.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.277.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.278": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.278.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.278.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.279": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.279.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.279.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.280": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.280.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.280.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.281": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.281.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.281.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.282": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.282.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.282.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.283": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.283.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.283.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:40, 30:42])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=6>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1  9.592759978668264 1.4245138783843014 ...  -6.788717 -3.2809281061057023
#[Out]#     2  5.298922426521624  4.523890585990861 ...  -7.308714  -3.197333707901482
#[Out]#     3   9.83779793718135  7.532732605273326 ... -6.8103976 -2.9753711498156985
#[Out]#     4 1.3667418185209934  9.632622461330628 ... -6.7526703 -3.2891106388941433
#[Out]#     5  7.236487083061761  9.692208568254909 ... -7.0272455 -2.6836747422814318
#[Out]#     6  9.743087182501863  9.469834048155098 ...  -6.834589  -3.300056125217063
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.284": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 256)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.284.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.284.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.285": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("fwh", 3)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.285.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.285.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.286": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("fwhm", 4, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.286.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.286.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.287": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("fwhm_", 5)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.287.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.287.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
fwhm_pix
#[Out]# 2.574
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.288": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.f(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.288.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.288.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.289": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.f(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.289.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.289.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.290": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.f(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.290.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.290.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.291": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fin(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.291.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.291.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.292": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fin(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.292.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.292.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.293": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fint(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.293.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.293.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.294": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fint(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.294.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.294.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.295": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fint(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.295.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.295.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.296": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fin(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.296.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.296.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.297": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.fin(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.297.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.297.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.298": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.298.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.298.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.299": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.299.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.299.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.300": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.300.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.300.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.301": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_s(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.301.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.301.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.302": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_st(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.302.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.302.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.303": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_sta(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.303.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.303.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.304": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_star(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.304.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.304.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.305": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_star(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.305.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.305.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.306": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_star(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.306.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.306.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.307": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.307.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.307.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.308": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.308.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.308.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.309": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.309.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.309.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.310": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.310.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.310.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:40, 30:42])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=6>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1  9.592759978668264 1.4245138783843014 ...  -6.788717 -3.2809281061057023
#[Out]#     2  5.298922426521624  4.523890585990861 ...  -7.308714  -3.197333707901482
#[Out]#     3   9.83779793718135  7.532732605273326 ... -6.8103976 -2.9753711498156985
#[Out]#     4 1.3667418185209934  9.632622461330628 ... -6.7526703 -3.2891106388941433
#[Out]#     5  7.236487083061761  9.692208568254909 ... -7.0272455 -2.6836747422814318
#[Out]#     6  9.743087182501863  9.469834048155098 ...  -6.834589  -3.300056125217063
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.311": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(np.nan_to_num(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 253)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.311.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.311.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.312": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mas)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 289)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.312.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.312.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.313": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOSmask=", 58, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.313.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.313.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.314": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 293)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.314.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.314.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.315": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.315.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.315.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.316": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.316.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.316.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.317": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.317.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.317.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.318": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.318.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.318.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.319": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.319.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.319.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.320": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.320.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.320.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.321": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.321.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.321.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.322": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.322.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.322.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.323": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.323.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.323.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.324": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.324.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.324.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.325": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.325.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.325.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.326": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.326.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.326.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.327": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.327.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.327.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.328": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 293)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.328.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.328.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.329": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 293)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.329.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.329.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.330": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 293)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.330.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.330.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.331": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 293)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.331.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.331.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.332": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.332.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.332.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.333": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.is)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.333.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.333.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.334": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.is)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.334.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.334.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.335": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isf)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.335.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.335.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.336": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfin)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.336.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.336.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.337": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfini)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.337.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.337.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.338": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinit)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.338.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.338.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.339": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.339.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.339.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.340": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.340.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.340.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.341": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.341.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.341.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.342": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.342.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.342.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.343": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.343.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.343.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.344": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.344.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.344.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.345": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.345.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.345.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.346": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.346.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.346.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.347": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.347.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.347.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.348": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite()\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.348.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.348.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.349": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.349.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.349.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.350": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.350.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.350.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.351": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.351.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.351.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.352": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.352.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.352.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.353": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.353.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.353.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.354": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.354.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.354.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.355": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.355.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.355.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:40, 30:42])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=5>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1  9.592759978668264 1.4245138783843014 ...  -6.788717 -3.2809281061057023
#[Out]#     2   9.83779793718135  7.532732605273326 ... -6.8103976 -2.9753711498156985
#[Out]#     3 1.3667418185209934  9.632622461330628 ... -6.7526703 -3.2891106388941433
#[Out]#     4  7.236487083061761  9.692208568254909 ... -7.0272455 -2.6836747422814318
#[Out]#     5  9.743087182501863  9.469834048155098 ...  -6.834589  -3.300056125217063
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.356": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=~np.isfinite(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 298)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.356.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.356.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.357": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=n(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 292)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.357.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.357.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.358": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinp", 60, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.358.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.358.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.359": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.359.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.359.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.360": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 294)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.360.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.360.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.361": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.361.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.361.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.362": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.362.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.362.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.363": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.363.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.363.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.364": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.364.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.364.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.365": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.365.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.365.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.366": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.366.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.366.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.367": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.is(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.367.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.367.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.368": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isn(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.368.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.368.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.369": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isna(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.369.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.369.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.370": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.370.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.370.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.371": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.371.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.371.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[28:40, 30:42])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=5>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1  9.592759978668264 1.4245138783843014 ...  -6.788717 -3.2809281061057023
#[Out]#     2   9.83779793718135  7.532732605273326 ... -6.8103976 -2.9753711498156985
#[Out]#     3 1.3667418185209934  9.632622461330628 ... -6.7526703 -3.2891106388941433
#[Out]#     4  7.236487083061761  9.692208568254909 ... -7.0272455 -2.6836747422814318
#[Out]#     5  9.743087182501863  9.469834048155098 ...  -6.834589  -3.300056125217063
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.372": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.372.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.372.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.373": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.373.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.373.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.374": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.374.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.374.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.375": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.375.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.375.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.376": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.376.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.376.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.377": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.377.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.377.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.378": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.378.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.378.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.379": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.379.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.379.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.380": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.380.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.380.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.381": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.381.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.381.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.382": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.382.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.382.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.383": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.383.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.383.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.384": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.384.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.384.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.385": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.385.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.385.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.386": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.386.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.386.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.387": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.387.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.387.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.388": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.388.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.388.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.389": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.389.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.389.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.390": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.390.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.390.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.391": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.391.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.391.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.392": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.392.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.392.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.393": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.393.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.393.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.394": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.394.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.394.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.395": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.395.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.395.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.396": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.396.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.396.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.397": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.397.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.397.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.398": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.398.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.398.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.399": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.399.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.399.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.400": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.400.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.400.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.401": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.401.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.401.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.402": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.402.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.402.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.403": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.403.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.403.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.404": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.404.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.404.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.405": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.405.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.405.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.406": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.406.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.406.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.407": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.407.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.407.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.408": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.408.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.408.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.409": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.409.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.409.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.410": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.410.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.410.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.411": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.411.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.411.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.412": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.412.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.412.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.413": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.413.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.413.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.414": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.414.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.414.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.415": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.415.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.415.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.416": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.416.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.416.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.417": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.417.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.417.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.418": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.418.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.418.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.419": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.419.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.419.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.420": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.420.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.420.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.421": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.421.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.421.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.422": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.422.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.422.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.423": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.423.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.423.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.424": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.424.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.424.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.425": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.425.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.425.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.426": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.426.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.426.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.427": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.427.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.427.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.428": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.428.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.428.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.429": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.429.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.429.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.430": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.430.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.430.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.431": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.431.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.431.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.432": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.432.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.432.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.433": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.433.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.433.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.434": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.434.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.434.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.435": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.435.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.435.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.436": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.436.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.436.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.437": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.437.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.437.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.438": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.438.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.438.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.439": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.439.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.439.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.440": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.440.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.440.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.441": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.441.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.441.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.442": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.442.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.442.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.443": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.443.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.443.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.444": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.444.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.444.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.445": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.445.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.445.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.446": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.446.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.446.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.447": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.447.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.447.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.448": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.448.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.448.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.449": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.449.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.449.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.450": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.450.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.450.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.451": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.451.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.451.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.452": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.452.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.452.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.453": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.453.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.453.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.454": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.454.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.454.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.455": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.455.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.455.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.456": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.456.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.456.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.457": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.457.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.457.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.458": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.458.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.458.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.459": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.459.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.459.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.460": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.460.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.460.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.461": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.461.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.461.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.462": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.462.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.462.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.463": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.463.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.463.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.464": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.464.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.464.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.465": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.465.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.465.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.466": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.466.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.466.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.467": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.467.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.467.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.468": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.468.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.468.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.469": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.469.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.469.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.470": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.470.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.470.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.471": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.471.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.471.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.472": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.472.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.472.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.473": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.473.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.473.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.474": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.474.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.474.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.475": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.475.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.475.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.476": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.476.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.476.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.477": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.477.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.477.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.478": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.478.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.478.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.479": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.479.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.479.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.480": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.480.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.480.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.481": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.481.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.481.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.482": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.482.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.482.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.483": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.483.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.483.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.484": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.484.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.484.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.485": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.485.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.485.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.486": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.486.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.486.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.487": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.487.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.487.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.488": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.488.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.488.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.489": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.489.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.489.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.490": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.490.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.490.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.491": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.491.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.491.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.492": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.492.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.492.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.493": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.493.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.493.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.494": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.494.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.494.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.495": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.495.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.495.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.496": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.496.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.496.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.497": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.497.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.497.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.498": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.498.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.498.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.499": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.499.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.499.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.500": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.500.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.500.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.501": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.501.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.501.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.502": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.502.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.502.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.503": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.503.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.503.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.504": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.504.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.504.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.505": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.505.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.505.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.506": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.506.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.506.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.507": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.507.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.507.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.508": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.508.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.508.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.509": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.509.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.509.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.510": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.510.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.510.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.511": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.511.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.511.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.512": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.512.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.512.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.513": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.513.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.513.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.514": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.514.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.514.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.515": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.515.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.515.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.516": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.516.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.516.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.517": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.517.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.517.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.518": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.518.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.518.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.519": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.519.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.519.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.520": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.520.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.520.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.521": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.521.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.521.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.522": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.522.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.522.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.523": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.523.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.523.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.524": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.524.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.524.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.525": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.525.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.525.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.526": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.526.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.526.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.527": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.527.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.527.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.528": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.528.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.528.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.529": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.529.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.529.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.530": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.530.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.530.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.531": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.531.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.531.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.532": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.532.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.532.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.533": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.533.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.533.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.534": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.534.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.534.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.535": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.535.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.535.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.536": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.536.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.536.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.537": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.537.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.537.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.538": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.538.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.538.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.539": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.539.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.539.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.540": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.540.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.540.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.541": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.541.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.541.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.542": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.542.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.542.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.543": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.543.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.543.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.544": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.544.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.544.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.545": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.545.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.545.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.546": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.546.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.546.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.547": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.547.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.547.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.548": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.548.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.548.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.549": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.549.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.549.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.550": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.550.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.550.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.551": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.551.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.551.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.552": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.552.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.552.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.553": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.553.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.553.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.554": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.554.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.554.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.555": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.555.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.555.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.556": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.556.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.556.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.557": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.557.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.557.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.558": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.558.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.558.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.559": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.559.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.559.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.560": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.560.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.560.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.561": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.561.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.561.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.562": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.562.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.562.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.563": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.563.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.563.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.564": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.564.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.564.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.565": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.565.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.565.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.566": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.566.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.566.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.567": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.567.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.567.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.568": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.568.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.568.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.569": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.569.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.569.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.570": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.570.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.570.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.571": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.571.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.571.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.572": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.572.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.572.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.573": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.573.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.573.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.574": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.574.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.574.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.575": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.575.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.575.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.576": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.576.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.576.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.577": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.577.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.577.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.578": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.578.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.578.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.579": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.579.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.579.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.580": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.580.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.580.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.581": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.581.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.581.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.582": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.582.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.582.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.583": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.583.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.583.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.584": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.584.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.584.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.585": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.585.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.585.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.586": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.586.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.586.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.587": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.587.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.587.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.588": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.588.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.588.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.589": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.589.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.589.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.590": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.590.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.590.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.591": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.591.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.591.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.592": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.592.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.592.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.593": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.593.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.593.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.594": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.594.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.594.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.595": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.595.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.595.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.596": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.596.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.596.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.597": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.597.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.597.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.598": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.598.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.598.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.599": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.599.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.599.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.600": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.600.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.600.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.601": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.601.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.601.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.602": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.602.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.602.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.603": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.603.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.603.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.604": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.604.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.604.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.605": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.605.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.605.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.606": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.606.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.606.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.607": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.607.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.607.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.608": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.608.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.608.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.609": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.609.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.609.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.610": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.610.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.610.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.611": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.611.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.611.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.612": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.612.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.612.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.613": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.613.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.613.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.614": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.614.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.614.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.615": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.615.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.615.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.616": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.616.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.616.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.617": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.617.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.617.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.618": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.618.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.618.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.619": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.619.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.619.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.620": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.620.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.620.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.621": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.621.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.621.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.622": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.622.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.622.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.623": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.623.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.623.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.624": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.624.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.624.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.625": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.625.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.625.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.626": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.626.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.626.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.627": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.627.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.627.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.628": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.628.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.628.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.629": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.629.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.629.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.630": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.630.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.630.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.631": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.631.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.631.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.632": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.632.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.632.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.633": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.633.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.633.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.634": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.634.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.634.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.635": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.635.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.635.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.636": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.636.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.636.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.637": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.637.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.637.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.638": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.638.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.638.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.639": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.639.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.639.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.640": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.640.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.640.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.641": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.641.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.641.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.642": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.642.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.642.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.643": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.643.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.643.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.644": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.644.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.644.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.645": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.645.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.645.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.646": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.646.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.646.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.647": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.647.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.647.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.648": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.648.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.648.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.649": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.649.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.649.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.650": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.650.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.650.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.651": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.651.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.651.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.652": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.652.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.652.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.653": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.653.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.653.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.654": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.654.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.654.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.655": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.655.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.655.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.656": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.656.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.656.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.657": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.657.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.657.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.658": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.658.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.658.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.659": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.659.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.659.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.660": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.660.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.660.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.661": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.661.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.661.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.662": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.662.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.662.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.663": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.663.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.663.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.664": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.664.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.664.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.665": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.665.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.665.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.666": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.666.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.666.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.667": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.667.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.667.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.668": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.668.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.668.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.669": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.669.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.669.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.670": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.670.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.670.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.671": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.671.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.671.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.672": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.672.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.672.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.673": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.673.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.673.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.674": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.674.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.674.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.675": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.675.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.675.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.676": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.676.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.676.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.677": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.677.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.677.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.678": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.678.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.678.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.679": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.679.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.679.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.680": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.680.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.680.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.681": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.681.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.681.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.682": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.682.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.682.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.683": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.683.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.683.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.684": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.684.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.684.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.685": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.685.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.685.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.686": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.686.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.686.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.687": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.687.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.687.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.688": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.688.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.688.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.689": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.689.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.689.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.690": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.690.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.690.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.691": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.691.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.691.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.692": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.692.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.692.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.693": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.693.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.693.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.694": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.694.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.694.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.695": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.695.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.695.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.696": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.696.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.696.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.697": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.697.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.697.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.698": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.698.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.698.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.699": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.699.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.699.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.700": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.700.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.700.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.701": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.701.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.701.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.702": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.702.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.702.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.703": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.703.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.703.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.704": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.704.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.704.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.705": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.705.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.705.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.706": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.706.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.706.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.707": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.707.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.707.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.708": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.708.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.708.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.709": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.709.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.709.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.710": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.710.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.710.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.711": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.711.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.711.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.712": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.712.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.712.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.713": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("f", 1)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.713.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.713.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.714": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.714.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.714.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.715": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.715.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.715.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.716": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.716.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.716.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.717": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.717.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.717.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.718": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.718.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.718.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.719": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astro", 9)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.719.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.719.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.720": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.720.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.720.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.721": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.721.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.721.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.722": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.722.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.722.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.723": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.c", 13)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.723.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.723.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.724": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.724.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.724.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.725": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.725.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.725.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.726": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.726.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.726.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.727": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.727.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.727.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.728": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.728.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.728.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.729": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convol", 19)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.729.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.729.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.730": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.730.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.730.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.731": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.731.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.731.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.732": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.732.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.732.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.733": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.733.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.733.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.734": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.734.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.734.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.735": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.735.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.735.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.736": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution i", 26)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.736.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.736.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.737": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.737.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.737.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.738": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.738.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.738.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.739": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.739.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.739.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.740": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.convolution import ", 32, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.740.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.740.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.741": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.convolution import ", 32, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.741.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.741.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.742": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.742.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.742.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.743": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.743.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.743.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.744": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.744.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.744.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.745": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.745.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.745.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.746": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import conv", 35)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.746.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.746.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.747": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.747.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.747.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.748": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.748.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.748.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.749": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.convolution import convolve_models_fft", 51, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.749.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.749.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.750": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.750.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.750.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.751": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.751.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.751.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.752": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.752.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.752.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.753": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.753.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.753.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.754": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.754.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.754.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.755": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.755.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.755.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.756": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.756.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.756.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.757": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.757.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.757.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.758": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.758.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.758.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.759": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.759.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.759.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.760": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.760.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.760.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.761": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.761.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.761.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.762": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.762.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.762.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.763": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.convolution import convolve_fft", 44, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.763.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.763.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.764": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.764.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.764.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.765": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.765.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.765.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.766": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.766.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.766.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.767": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.767.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.767.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.768": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.768.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.768.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.769": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.769.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.769.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.770": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.770.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.770.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.771": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.771.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.771.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.772": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.772.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.772.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.773": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.773.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.773.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.774": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.774.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.774.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.775": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.775.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.775.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.776": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.776.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.776.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.777": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.777.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.777.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.778": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.778.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.778.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.779": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.779.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.779.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.780": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.780.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.780.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.781": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, G", 43)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.781.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.781.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.782": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.782.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.782.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.783": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.783.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.783.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.784": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.convolution import convolve, Gaussian1DKernel", 58, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.784.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.784.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.785": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.785.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.785.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.786": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.786.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.786.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.787": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.787.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.787.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.788": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.788.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.788.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.789": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.789.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.789.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.790": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.790.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.790.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.791": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.791.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.791.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.792": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.792.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.792.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.793": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.793.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.793.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.794": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.794.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.794.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.795": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.795.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.795.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.796": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.796.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.796.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.797": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.797.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.797.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.798": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.798.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.798.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.799": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.799.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.799.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.800": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.800.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.800.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.801": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.801.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.801.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.802": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.802.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.802.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.803": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.803.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.803.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.804": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.804.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.804.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.805": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.805.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.805.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.806": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.806.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.806.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.807": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.807.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.807.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.808": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.808.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.808.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.809": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.809.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.809.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.810": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2", 51)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.810.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.810.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.811": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.811.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.811.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.812": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.812.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.812.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.813": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.813.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.813.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.814": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.814.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.814.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.815": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.815.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.815.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.816": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.816.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.816.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.817": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.817.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.817.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.818": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.818.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.818.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.819": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.819.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.819.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.820": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.820.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.820.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.821": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.821.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.821.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.822": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.822.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.822.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.823": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.823.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.823.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.824": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.824.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.824.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.825": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.825.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.825.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.826": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.826.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.826.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.827": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.827.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.827.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.828": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.828.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.828.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.829": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.829.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.829.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.830": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.830.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.830.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.831": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.831.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.831.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.832": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.832.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.832.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.833": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.833.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.833.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.834": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.834.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.834.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.835": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.835.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.835.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.836": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.836.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.836.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.837": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.837.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.837.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.838": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.838.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.838.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.839": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.839.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.839.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.840": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.840.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.840.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.841": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.841.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.841.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.842": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.842.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.842.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.843": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.843.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.843.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.844": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.844.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.844.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.845": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.845.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.845.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.846": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.846.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.846.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.847": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.847.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.847.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.848": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.848.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.848.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.849": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.849.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.849.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.850": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.850.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.850.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.851": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.851.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.851.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.852": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.852.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.852.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.853": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.853.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.853.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.854": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.854.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.854.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.855": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.855.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.855.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.856": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.856.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.856.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.857": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.857.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.857.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.858": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.858.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.858.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.859": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.859.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.859.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.860": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.860.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.860.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.861": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.861.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.861.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.862": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.862.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.862.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.863": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.863.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.863.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.864": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.864.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.864.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.865": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.865.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.865.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.866": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.866.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.866.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.867": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.867.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.867.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.868": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.868.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.868.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.869": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.869.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.869.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.870": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.870.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.870.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.871": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.871.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.871.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.872": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.872.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.872.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.873": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.873.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.873.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.874": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.874.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.874.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.875": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.875.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.875.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.876": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.876.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.876.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.877": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.877.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.877.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.878": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.878.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.878.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.879": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.879.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.879.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.880": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.880.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.880.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.881": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.881.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.881.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.882": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.882.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.882.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.883": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.883.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.883.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.884": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.884.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.884.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.885": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.885.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.885.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.886": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.886.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.886.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.887": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.887.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.887.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.888": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.888.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.888.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.889": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.889.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.889.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.890": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.890.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.890.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.891": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.891.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.891.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.892": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.892.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.892.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.893": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.893.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.893.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.894": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.894.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.894.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.895": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.895.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.895.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.896": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.896.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.896.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.897": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.897.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.897.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.898": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.898.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.898.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.899": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.899.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.899.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.900": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.900.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.900.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.901": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.901.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.901.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.902": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.902.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.902.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.903": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.903.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.903.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.904": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.904.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.904.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.905": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.905.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.905.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.906": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.906.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.906.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.907": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.907.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.907.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.908": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.908.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.908.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.909": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.909.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.909.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.910": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.910.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.910.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.911": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.911.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.911.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.912": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.912.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.912.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.913": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.913.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.913.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.914": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.914.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.914.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.915": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.915.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.915.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.916": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.916.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.916.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.917": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.917.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.917.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.918": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.918.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.918.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.919": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.919.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.919.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.920": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.920.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.920.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.921": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.921.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.921.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.922": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.922.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.922.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.923": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.923.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.923.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.924": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.924.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.924.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.925": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.925.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.925.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.926": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.926.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.926.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.927": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.927.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.927.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.928": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.928.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.928.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.929": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.929.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.929.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.930": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.930.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.930.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.931": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.931.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.931.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.932": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.932.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.932.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.933": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.933.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.933.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.934": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.934.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.934.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.935": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.935.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.935.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.936": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.936.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.936.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.937": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.937.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.937.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.938": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.938.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.938.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.939": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.939.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.939.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.940": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.940.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.940.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.941": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.941.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.941.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.942": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(w\n", 86)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.942.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.942.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.943": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.943.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.943.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.944": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.944.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.944.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.945": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.945.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.945.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.946": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.946.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.946.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.947": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(width\n", 90)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.947.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.947.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.948": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.948.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.948.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.949": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(width\n", 90)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.949.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.949.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.950": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.950.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.950.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.951": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(width\n", 90)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.951.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.951.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.952": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.952.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.952.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.953": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(widt\n", 89)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.953.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.953.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.954": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.954.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.954.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.955": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(widt\n", 89)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.955.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.955.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.956": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.956.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.956.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.957": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(wid\n", 88)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.957.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.957.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.958": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.958.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.958.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.959": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(wid\n", 88)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.959.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.959.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.960": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.960.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.960.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.961": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(wi\n", 87)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.961.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.961.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.962": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.962.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.962.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.963": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.963.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.963.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.964": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.964.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.964.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.965": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.965.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.965.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.966": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.966.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.966.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.967": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.967.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.967.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.968": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.968.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.968.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.969": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.969.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.969.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.970": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.970.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.970.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.971": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.971.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.971.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.972": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.972.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.972.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.973": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.973.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.973.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.974": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.974.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.974.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.975": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.975.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.975.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.976": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.976.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.976.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.977": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.977.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.977.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.978": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.978.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.978.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.979": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.979.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.979.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.980": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.980.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.980.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.981": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.981.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.981.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.982": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.982.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.982.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.983": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.983.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.983.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.984": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.984.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.984.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.985": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.985.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.985.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.986": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.986.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.986.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.987": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.987.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.987.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.988": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.988.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.988.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.989": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.989.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.989.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.990": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.990.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.990.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.991": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.991.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.991.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.992": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.992.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.992.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.993": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.993.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.993.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.994": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.994.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.994.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.995": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.995.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.995.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.996": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.996.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.996.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.997": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.997.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.997.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.998": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.998.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.998.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.999": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.999.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.999.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1000": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1000.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1000.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1001": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1001.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1001.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1002": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1002.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1002.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1003": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1003.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1003.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1004": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1004.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1004.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1005": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1005.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1005.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1006": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1006.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1006.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1007": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1007.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1007.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1008": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1008.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1008.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1009": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1009.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1009.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1010": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1010.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1010.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1011": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1011.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1011.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1012": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1012.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1012.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1013": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1013.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1013.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1014": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1014.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1014.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1015": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1015.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1015.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1016": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1016.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1016.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1017": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1017.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1017.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1018": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1018.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1018.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1019": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1019.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1019.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1020": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1020.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1020.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1021": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1021.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1021.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1022": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1022.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1022.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1023": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1023.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1023.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1024": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1024.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1024.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1025": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1025.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1025.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1026": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1026.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1026.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1027": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1027.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1027.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1028": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1028.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1028.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1029": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1029.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1029.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1030": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1030.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1030.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1031": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1031.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1031.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1032": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1032.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1032.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1033": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1033.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1033.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1034": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1034.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1034.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1035": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1035.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1035.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1036": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1036.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1036.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1037": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1037.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1037.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1038": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1038.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1038.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1039": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1039.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1039.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1040": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1040.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1040.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1041": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1041.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1041.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1042": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1042.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1042.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1043": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1043.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1043.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1044": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1044.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1044.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1045": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1045.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1045.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1046": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1046.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1046.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1047": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1047.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1047.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1048": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1048.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1048.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1049": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1049.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1049.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1050": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1050.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1050.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1051": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1051.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1051.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1052": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1052.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1052.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1053": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1053.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1053.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1054": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1054.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1054.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1055": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1055.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1055.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1056": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1056.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1056.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1057": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1057.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1057.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1058": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1058.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1058.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1059": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1059.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1059.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1060": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1060.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1060.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1061": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1061.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1061.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1062": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1062.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1062.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1063": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1063.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1063.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1064": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1064.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1064.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1065": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1065.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1065.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1066": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1066.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1066.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1067": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1067.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1067.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1068": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1068.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1068.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1069": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1069.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1069.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1070": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1070.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1070.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1071": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1071.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1071.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1072": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1072.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1072.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1073": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1073.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1073.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1074": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1074.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1074.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1075": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1075.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1075.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1076": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1076.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1076.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1077": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1077.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1077.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1078": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1078.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1078.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1079": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1079.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1079.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1080": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1080.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1080.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1081": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1081.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1081.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1082": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1082.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1082.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1083": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1083.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1083.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1084": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1084.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1084.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1085": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1085.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1085.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1086": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1086.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1086.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1087": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1087.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1087.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1088": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1088.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1088.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1089": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1089.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1089.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1090": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1090.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1090.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1091": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1091.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1091.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1092": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1092.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1092.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1093": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1093.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1093.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1094": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1094.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1094.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1095": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1095.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1095.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1096": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1096.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1096.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1097": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1097.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1097.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1098": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1098.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1098.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1099": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1099.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1099.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1100": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1100.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1100.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1101": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1101.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1101.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1102": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1102.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1102.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1103": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1103.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1103.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1104": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1104.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1104.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1105": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1105.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1105.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1106": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1106.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1106.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1107": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1107.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1107.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1108": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1108.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1108.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1109": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1109.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1109.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1110": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1110.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1110.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1111": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1111.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1111.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1112": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1112.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1112.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1113": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1113.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1113.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1114": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1114.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1114.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1115": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1115.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1115.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1116": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1116.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1116.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1117": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1117.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1117.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1118": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1118.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1118.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1119": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1119.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1119.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1120": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1120.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1120.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1121": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1121.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1121.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1122": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1122.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1122.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1123": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1123.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1123.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1124": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1124.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1124.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1125": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1125.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1125.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1126": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1126.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1126.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1127": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1127.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1127.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1128": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1128.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1128.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1129": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1129.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1129.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1130": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1130.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1130.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1131": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1131.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1131.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1132": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1132.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1132.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1133": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1133.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1133.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1134": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1134.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1134.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1135": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1135.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1135.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1136": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1136.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1136.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1137": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1137.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1137.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1138": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1138.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1138.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1139": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1139.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1139.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1140": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1140.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1140.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1141": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1141.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1141.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1142": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1142.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1142.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1143": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1143.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1143.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1144": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1144.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1144.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1145": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1145.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1145.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1146": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1146.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1146.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1147": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1147.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1147.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1148": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1148.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1148.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1149": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1149.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1149.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1150": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1150.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1150.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1151": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1151.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1151.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1152": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1152.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1152.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1153": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1153.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1153.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1154": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1154.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1154.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1155": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1155.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1155.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1156": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1156.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1156.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1157": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1157.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1157.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1158": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1158.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1158.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1159": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1159.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1159.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1160": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1160.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1160.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1161": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\nc\n", 134)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1161.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1161.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1162": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1162.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1162.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1163": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1163.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1163.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1164": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1164.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1164.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1165": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1165.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1165.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1166": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1166.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1166.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1167": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1167.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1167.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1168": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1168.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1168.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1169": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1169.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1169.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1170": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1170.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1170.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1171": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1171.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1171.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1172": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1172.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1172.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1173": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1173.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1173.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1174": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1174.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1174.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1175": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\ncutout[np\n", 141)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1175.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1175.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1176": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1176.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1176.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1177": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1177.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1177.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1178": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1178.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1178.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1179": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1179.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1179.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1180": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\ncutout[np.\n", 143)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1180.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1180.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1181": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1181.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1181.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1182": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1182.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1182.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1183": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[28:40, 30:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42], mask=np.isnan(cutout[28:40, 30:42]))\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 295)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1183.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1183.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1184": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\ncutout[np.isn\n", 146)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1184.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1184.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1185": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\ncutout[np.isnan(c\n", 150)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1185.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1185.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1186": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.concontinue", 24, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1186.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1186.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1187": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.concompile", 23, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1187.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1187.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1188": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.concutout", 22, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1188.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1188.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1189": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("from astropy.concallable", 24, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1189.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1189.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1190": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\ncutout[np.isnan(cutout)] = sm\n", 161)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1190.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1190.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
from astropy.convolution import convolve, Gaussian2DKernel
kernel = Gaussian2DKernel(x_stddev=1)
smoothed = convolve(cutout, kernel)
cutout[np.isnan(cutout)] = smoothed[np.isnan(cutout)]
cutout[28:38, 30:40]
#[Out]# array([[30.964502, 32.07793 , 31.690294, 31.534588, 32.212914, 32.7543  ,
#[Out]#         32.173683, 32.727886, 31.668354, 32.239162],
#[Out]#        [31.059711, 31.558249, 31.093742, 31.249773, 32.93707 , 33.14296 ,
#[Out]#         32.81087 , 32.932922, 33.31726 , 32.379574],
#[Out]#        [30.949366, 31.79206 , 31.476746, 31.357334, 33.689957, 37.110947,
#[Out]#         35.78524 , 33.398914, 33.09975 , 32.7724  ],
#[Out]#        [30.621435, 32.21477 , 32.205322, 33.468307, 37.998882, 41.522045,
#[Out]#         38.642937, 34.92624 , 32.942623, 33.111748],
#[Out]#        [31.585247, 30.827196, 30.6575  , 32.793053, 36.152946, 40.279076,
#[Out]#         38.89247 , 34.438087, 33.09562 , 33.576077],
#[Out]#        [31.371939, 31.61736 , 31.643291, 31.895227, 33.49506 , 34.847324,
#[Out]#         35.33985 , 32.82683 , 32.943775, 32.654346],
#[Out]#        [30.238377, 31.43596 , 30.916313, 31.385044, 31.088085, 31.70609 ,
#[Out]#         31.539515, 32.015682, 32.974174, 33.61422 ],
#[Out]#        [31.178288, 30.914255, 30.29068 , 30.532543, 30.808067, 31.755266,
#[Out]#         31.501379, 31.65471 , 32.356205, 32.338024],
#[Out]#        [30.728382, 31.253284, 30.996904, 30.952894, 31.587542, 31.811686,
#[Out]#         31.30703 , 31.744236, 32.786343, 32.542862],
#[Out]#        [32.077297, 31.74161 , 31.369831, 31.635403, 30.920923, 31.90437 ,
#[Out]#         32.000687, 33.29016 , 32.24078 , 32.22452 ]], dtype='>f4')
plt.imshow(cutout[28:40, 30:42])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[28:40, 30:42])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=6>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1 1.3834754751426033 1.3766336767547456 ...  -6.758904  -3.283065957891363
#[Out]#     2  9.592759978668264 1.4245138783843014 ...  -6.788717 -3.2809281061057023
#[Out]#     3   9.83779793718135  7.532732605273326 ... -6.8103976 -2.9753711498156985
#[Out]#     4 1.3667418185209934  9.632622461330628 ... -6.7526703 -3.2891106388941433
#[Out]#     5  7.236487083061761  9.692208568254909 ... -7.0272455 -2.6836747422814318
#[Out]#     6  9.743087182501863  9.469834048155098 ...  -6.834589  -3.300056125217063
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1191": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 27)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1191.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1191.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1192": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 27)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1192.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1192.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1193": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1193.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1193.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1194": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1194.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1194.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1195": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1195.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1195.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1196": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1196.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1196.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1197": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1197.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1197.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1198": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1198.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1198.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1199": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1199.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1199.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1200": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1200.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1200.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1201": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1201.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1201.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1202": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 2s5:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1202.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1202.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1203": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:45, 25:42])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[28:40, 30:42])\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 28)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1203.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1203.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
plt.imshow(cutout[25:45, 25:45])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[25:45, 25:45])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=14>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag    
#[Out]# int64      float64            float64       ...  float32         float64      
#[Out]# ----- ------------------ ------------------ ... ---------- -------------------
#[Out]#     1 1.3792772720277382 1.3289116229918991 ...  -6.737035 -3.2491076683716917
#[Out]#     2 6.7536631165532945 1.3530471475360544 ...  -7.015105  -2.665466283773046
#[Out]#     3 10.420713859252244  1.386685870975139 ...  -7.009408 -2.5764187488932255
#[Out]#     4 14.311717170285512  1.381584824006165 ... -7.0122294 -2.6244782259769757
#[Out]#     5 17.595869591931425 1.3885668761920182 ... -6.7746315 -3.2681513142053205
#[Out]#     6 1.3900803820516912  5.210997350986852 ... -6.9659834    -2.5520300581271
#[Out]#     7 10.228702384362602  6.371498642092447 ... -7.3512096   -2.26345661680451
#[Out]#     8  17.29126412422795 10.473137301428565 ... -7.1729035 -2.8924437036592265
#[Out]#     9 12.147334374097737  16.88766523261172 ... -7.3002253 -0.3617905969380691
#[Out]#    10 1.4254460974193175 17.650735857798523 ... -6.7653537 -3.2984536868010625
#[Out]#    11  3.643725837004662 17.658787291811755 ... -7.0224476   -2.62188849164461
#[Out]#    12 10.160251867851565 17.706294904635513 ... -7.0623946  -2.727613855946347
#[Out]#    13 12.590977626365534 17.677788989158724 ... -7.0819244 -2.6951187464707003
#[Out]#    14 17.575033544952536 17.620726679867776 ...  -6.792236  -3.283159524627162
plt.imshow(cutout[25:50, 25:50])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[25:50, 25:50])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=23>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag     
#[Out]# int64      float64            float64       ...  float32         float64       
#[Out]# ----- ------------------ ------------------ ... ---------- --------------------
#[Out]#     1 1.3792772720277382 1.3289116229918991 ...  -6.737035  -3.2491076683716917
#[Out]#     2 6.7536631165532945 1.3530471475360544 ...  -7.015105   -2.665466283773046
#[Out]#     3 10.420713859252244  1.386685870975139 ...  -7.009408  -2.5764187488932255
#[Out]#     4 14.311717170285512  1.381584824006165 ... -7.0122294  -2.6244782259769757
#[Out]#     5 16.857569928163347 1.3813821088900589 ... -7.0139546   -2.627116237793791
#[Out]#     6 22.670830772741493  1.331679620987284 ... -6.7974133    -3.35424694269016
#[Out]#     7  22.63564312942178  3.991798347636448 ...   -7.02308  -2.5867539978718175
#[Out]#     8 1.3900803820516912  5.210997350986852 ... -6.9659834     -2.5520300581271
#[Out]#     9 10.228702384362602  6.371498642092447 ... -7.3512096    -2.26345661680451
#[Out]#   ...                ...                ... ...        ...                  ...
#[Out]#    14 22.570763507216917 16.731585929964876 ... -6.9676876  -2.5108164732843443
#[Out]#    15 1.4267655711868314  18.22349429901692 ... -7.0041533  -2.5794756859167025
#[Out]#    16  3.667569668263566  18.64741107784339 ... -7.2589436 -0.05366344893150274
#[Out]#    17 10.893474047809809 19.225444192086155 ...  -7.331003   -0.763186396451086
#[Out]#    18 1.4132928899083352 22.585219414611274 ...  -6.702964  -3.1884537468476744
#[Out]#    19  6.742155126137676 22.555637496915185 ... -6.9988356  -2.5764542422136842
#[Out]#    20  11.18078991656377 22.574889270932946 ...  -7.045772    -2.63579552709819
#[Out]#    21 18.030659988606832  22.48818859432525 ...  -6.975814   -2.621154461345504
#[Out]#    22 22.649774805175163 22.276100618544667 ...  -6.651447    -3.07849173923685
#[Out]#    23 20.299377890417478 21.582184693814497 ... -6.5624433  -2.6887658566938084
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1204": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\nm\ncutout[np.isnan(cutout)] = smoothed[np.isnan(cutout)]\ncutout[28:38, 30:40]\n", 134)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1204.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1204.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1205": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("from astropy.convolution import convolve, Gaussian2DKernel\nkernel = Gaussian2DKernel(x_stddev=1)\nsmoothed = convolve(cutout, kernel)\nmask = \ncutout[m] = smoothed[np.isnan(cutout)]\ncutout[28:38, 30:40]\n", 149)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1205.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1205.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1206": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], m)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 287)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1206.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1206.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1207": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_inspect("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOSmask=", 58, 0)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1207.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1207.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1208": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskma)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1208.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1208.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1209": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskmas)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1209.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1209.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1210": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskmas)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1210.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1210.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1211": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskmas)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1211.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1211.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1212": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskma)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1212.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1212.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1213": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], maskma)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1213.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1213.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1214": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], mask)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 291)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1214.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1214.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
def __jupyter_exec_background__():
    from IPython.display import display
    from threading import Thread
    from traceback import format_exc

    # First send a dummy response to get the display id.
    # Later we'll send the real response with the actual data.
    # And that can happen much later even after the execution completes,
    # as that response will be sent from a bg thread.
    output = display({"application/vnd.vscode.bg.execution.1215": ""}, raw=True, display_id=True)

    def do_implementation():
        return get_ipython().kernel.do_complete("plt.imshow(cutout[25:50, 25:50])\ndaofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,\n                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,\n                                  sharplo=0.10, sharphi=1.90)\n\nfin = daofind_tuned.find_stars(cutout[25:50, 25:50], mask=m)\nplt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')\nfin", 292)

    def bg_main():
        try:
            output.update({"application/vnd.vscode.bg.execution.1215.result": do_implementation()}, raw=True)
        except Exception as e:
            output.update({"application/vnd.vscode.bg.execution.1215.error": format_exc()}, raw=True)


    Thread(target=bg_main, daemon=True).start()


__jupyter_exec_background__()
del __jupyter_exec_background__
import glob
from astropy.table import Table
from astropy.io import fits
from astropy.wcs import WCS
from astropy.coordinates import SkyCoord
import matplotlib.pyplot as plt
from astropy.visualization import simple_norm
import numpy as np
from jwst.datamodels import dqflags
from scipy.ndimage import label, find_objects, center_of_mass, sum_labels


def main(xpos, ypos, size=100, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' ):
    #jw06151001001_03109_00007_nrcblong_cal.fits
    imgdir = f'/orange/adamginsburg/jwst/w51/{filter.upper()}/'
    img_filename = imgdir + '/pipeline/'+f'jw06151001001_0{vgroup}_{expnum}_{module}_cal.fits'
    img = fits.open(img_filename)

    fig = plt.figure(figsize=(30,15))
    ax1 = fig.add_subplot(1,2,1)
    ax2 = fig.add_subplot(1,2,2)
    wcs_img = WCS(img[1].header)
    norm = simple_norm(img[1].data, 'asinh', min_percent=1, max_percent=99)
    ax1.imshow(img[1].data, norm=norm, origin='lower', cmap='inferno')
    square = plt.Rectangle((xpos - size/2, ypos - size/2), size, size, edgecolor='cyan', facecolor='none', lw=2)
    ax1.add_patch(square)
    ax1.set_title(f'Image: {img_filename}', fontsize=16)
    # f480m_nrcb_visit001_vgroup3109_exp00007_daophot_basic.fits
    wav = int(filter.lower()[1:4])
    if wav > 300:
        module2 = module[:-4]  #nrca or nrcb
    else:
        module2 = module
    cat_basic_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_basic.fits'
    cat_refined_filename = imgdir + f'{filter.lower()}_{module2}_visit001_vgroup{vgroup}_exp{expnum}_daophot_refined.fits'

    #jw06151001001_03109_00007_nrcblong_align_o001_crf_satstar_catalog_newnewnewnew.fits
    sat_cat_filename = imgdir + '/pipeline/' f'jw06151001001_0{vgroup}_{expnum}_{module}_align_o001_crf_satstar_catalog_newnewnewnew.fits'

    cutout_xmin = int(xpos - size/2)
    cutout_xmax = int(xpos + size/2)
    cutout_ymin = int(ypos - size/2)
    cutout_ymax = int(ypos + size/2)
    cutout = img[1].data[cutout_ymin:cutout_ymax, cutout_xmin:cutout_xmax]
    norm = simple_norm(cutout, 'asinh', min_percent=1, max_percent=99)

    ax2.imshow(cutout, norm=norm, origin='lower', cmap='inferno')
    ax2.set_title(f'Cutout centered at ({xpos}, {ypos})', fontsize=16)
    
    cat_basic = Table.read(cat_basic_filename)
    cat_refined = Table.read(cat_refined_filename)
    sat_cat = Table.read(sat_cat_filename)

    xpos_basic = cat_basic['x_fit']
    ypos_basic = cat_basic['y_fit']
    xpos_refined = cat_refined['x_fit']
    ypos_refined = cat_refined['y_fit']
    print(sat_cat.colnames)

    xpos_sat = sat_cat['xcentroid']
    ypos_sat = sat_cat['ycentroid']
    ax1.scatter(xpos_sat, ypos_sat, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)

    ax2.scatter(xpos_basic-cutout_xmin, ypos_basic-cutout_ymin, s=500, edgecolor='blue', facecolor='none', label='Basic Catalog', lw=2)
    ax2.scatter(xpos_refined-cutout_xmin, ypos_refined-cutout_ymin, s=500, edgecolor='green', facecolor='none', label='Refined Catalog', lw=2)
    ax2.scatter(xpos_sat-cutout_xmin, ypos_sat-cutout_ymin, s=500, edgecolor='red', facecolor='none', label='Saturated Stars Catalog', lw=2)
    ax2.set_xlim(0, size)
    ax2.set_ylim(0, size)

    dqarr = fits.open(img_filename)['DQ'].data
    is_saturated = (dqarr & dqflags.pixel['SATURATED']) != 0
    labeled_array, num_features = label(is_saturated)
    centers = center_of_mass(is_saturated, labeled_array, range(1, num_features + 1))
    for center in centers:
        ycen, xcen = center
        if cutout_xmin <= xcen < cutout_xmax and cutout_ymin <= ycen < cutout_ymax:
            ax2.scatter(xcen - cutout_xmin, ycen - cutout_ymin, s=800, edgecolor='magenta', facecolor='none', label='Saturated Star Center', lw=2)
    return img['SCI'].data, img['ERR'].data, cutout
data, err, cutout = main(450, 2000, size=50, filter='f480m', module='nrcblong', vgroup='3109', expnum='00001' )   


from photutils.detection import DAOStarFinder
from astropy import stats
basepath = '/orange/adamginsburg/jwst/w51/'
filtered_errest = np.nanmedian(err)

fwhm_tbl = Table.read(f'{basepath}/reduction/fwhm_table.ecsv')

filtername = 'F480M'

row = fwhm_tbl[fwhm_tbl['Filter'] == filtername]
fwhm = fwhm_arcsec = float(row['PSF FWHM (arcsec)'][0])
fwhm_pix = float(row['PSF FWHM (pixel)'][0])
print(filtered_errest)

daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix,# roundhi=1.0, roundlo=-1.0,
                                  #sharplo=0.30, sharphi=1.40)
                                  )


finstars = daofind_tuned(np.nan_to_num(cutout))
finstars.pprint(max_lines=-1)
cutout[28:38, 30:40]
#[Out]# array([[30.964502, 32.07793 , 31.690294, 31.534588, 32.212914, 32.7543  ,
#[Out]#         32.173683, 32.727886, 31.668354, 32.239162],
#[Out]#        [31.059711, 31.558249, 31.093742, 31.249773, 32.93707 , 33.14296 ,
#[Out]#         32.81087 , 32.932922, 33.31726 , 32.379574],
#[Out]#        [30.949366, 31.79206 , 31.476746, 31.357334, 33.689957, 37.110947,
#[Out]#         35.78524 , 33.398914, 33.09975 , 32.7724  ],
#[Out]#        [30.621435, 32.21477 , 32.205322,       nan, 37.998882, 41.522045,
#[Out]#         38.642937, 34.92624 , 32.942623, 33.111748],
#[Out]#        [31.585247, 30.827196, 30.6575  , 32.793053, 36.152946, 40.279076,
#[Out]#         38.89247 , 34.438087, 33.09562 , 33.576077],
#[Out]#        [31.371939, 31.61736 , 31.643291, 31.895227, 33.49506 , 34.847324,
#[Out]#         35.33985 , 32.82683 , 32.943775, 32.654346],
#[Out]#        [30.238377, 31.43596 , 30.916313, 31.385044, 31.088085, 31.70609 ,
#[Out]#         31.539515, 32.015682, 32.974174, 33.61422 ],
#[Out]#        [31.178288, 30.914255, 30.29068 , 30.532543, 30.808067, 31.755266,
#[Out]#         31.501379, 31.65471 , 32.356205, 32.338024],
#[Out]#        [30.728382, 31.253284, 30.996904, 30.952894, 31.587542, 31.811686,
#[Out]#         31.30703 , 31.744236, 32.786343, 32.542862],
#[Out]#        [32.077297, 31.74161 , 31.369831, 31.635403, 30.920923, 31.90437 ,
#[Out]#         32.000687, 33.29016 , 32.24078 , 32.22452 ]], dtype='>f4')
from astropy.convolution import convolve, Gaussian2DKernel
kernel = Gaussian2DKernel(x_stddev=1)
smoothed = convolve(cutout, kernel)
mask = np.isnan(cutout)
cutout[mask] = smoothed[mask]
cutout[28:38, 30:40]
#[Out]# array([[30.964502, 32.07793 , 31.690294, 31.534588, 32.212914, 32.7543  ,
#[Out]#         32.173683, 32.727886, 31.668354, 32.239162],
#[Out]#        [31.059711, 31.558249, 31.093742, 31.249773, 32.93707 , 33.14296 ,
#[Out]#         32.81087 , 32.932922, 33.31726 , 32.379574],
#[Out]#        [30.949366, 31.79206 , 31.476746, 31.357334, 33.689957, 37.110947,
#[Out]#         35.78524 , 33.398914, 33.09975 , 32.7724  ],
#[Out]#        [30.621435, 32.21477 , 32.205322, 33.468307, 37.998882, 41.522045,
#[Out]#         38.642937, 34.92624 , 32.942623, 33.111748],
#[Out]#        [31.585247, 30.827196, 30.6575  , 32.793053, 36.152946, 40.279076,
#[Out]#         38.89247 , 34.438087, 33.09562 , 33.576077],
#[Out]#        [31.371939, 31.61736 , 31.643291, 31.895227, 33.49506 , 34.847324,
#[Out]#         35.33985 , 32.82683 , 32.943775, 32.654346],
#[Out]#        [30.238377, 31.43596 , 30.916313, 31.385044, 31.088085, 31.70609 ,
#[Out]#         31.539515, 32.015682, 32.974174, 33.61422 ],
#[Out]#        [31.178288, 30.914255, 30.29068 , 30.532543, 30.808067, 31.755266,
#[Out]#         31.501379, 31.65471 , 32.356205, 32.338024],
#[Out]#        [30.728382, 31.253284, 30.996904, 30.952894, 31.587542, 31.811686,
#[Out]#         31.30703 , 31.744236, 32.786343, 32.542862],
#[Out]#        [32.077297, 31.74161 , 31.369831, 31.635403, 30.920923, 31.90437 ,
#[Out]#         32.000687, 33.29016 , 32.24078 , 32.22452 ]], dtype='>f4')
plt.imshow(cutout[25:50, 25:50])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[25:50, 25:50], mask=mask)
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
plt.imshow(cutout[25:50, 25:50])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=5.0, roundlo=-5.0,
                                  sharplo=0.10, sharphi=1.90)

fin = daofind_tuned.find_stars(cutout[25:50, 25:50], mask=mask[25:50, 25:50])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=23>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag     
#[Out]# int64      float64            float64       ...  float32         float64       
#[Out]# ----- ------------------ ------------------ ... ---------- --------------------
#[Out]#     1 1.3792772720277382 1.3289116229918991 ...  -6.737035  -3.2491076683716917
#[Out]#     2 6.7536631165532945 1.3530471475360544 ...  -7.015105   -2.665466283773046
#[Out]#     3 10.420713859252244  1.386685870975139 ...  -7.009408  -2.5764187488932255
#[Out]#     4 14.311717170285512  1.381584824006165 ... -7.0122294  -2.6244782259769757
#[Out]#     5 16.857569928163347 1.3813821088900589 ... -7.0139546   -2.627116237793791
#[Out]#     6 22.670830772741493  1.331679620987284 ... -6.7974133    -3.35424694269016
#[Out]#     7  22.63564312942178  3.991798347636448 ...   -7.02308  -2.5867539978718175
#[Out]#     8 1.3900803820516912  5.210997350986852 ... -6.9659834     -2.5520300581271
#[Out]#     9 10.228702384362602  6.371498642092447 ... -7.3512096    -2.26345661680451
#[Out]#   ...                ...                ... ...        ...                  ...
#[Out]#    14 22.570763507216917 16.731585929964876 ... -6.9676876  -2.5108164732843443
#[Out]#    15 1.4267655711868314  18.22349429901692 ... -7.0041533  -2.5794756859167025
#[Out]#    16  3.667569668263566  18.64741107784339 ... -7.2589436 -0.05366344893150274
#[Out]#    17 10.893474047809809 19.225444192086155 ...  -7.331003   -0.763186396451086
#[Out]#    18 1.4132928899083352 22.585219414611274 ...  -6.702964  -3.1884537468476744
#[Out]#    19  6.742155126137676 22.555637496915185 ... -6.9988356  -2.5764542422136842
#[Out]#    20  11.18078991656377 22.574889270932946 ...  -7.045772    -2.63579552709819
#[Out]#    21 18.030659988606832  22.48818859432525 ...  -6.975814   -2.621154461345504
#[Out]#    22 22.649774805175163 22.276100618544667 ...  -6.651447    -3.07849173923685
#[Out]#    23 20.299377890417478 21.582184693814497 ... -6.5624433  -2.6887658566938084
plt.imshow(cutout[25:50, 25:50])
daofind_tuned = DAOStarFinder(threshold=4 * filtered_errest,
                                  fwhm=fwhm_pix, roundhi=1.0, roundlo=-1.0,
                                  sharplo=0.30, sharphi=1.40)

fin = daofind_tuned.find_stars(cutout[25:50, 25:50], mask=mask[25:50, 25:50])
plt.scatter(fin['xcentroid'], fin['ycentroid'], edgecolor='red', facecolor='none')
fin
#[Out]# <QTable length=6>
#[Out]#   id      xcentroid          ycentroid      ...    mag         daofind_mag     
#[Out]# int64      float64            float64       ...  float32         float64       
#[Out]# ----- ------------------ ------------------ ... ---------- --------------------
#[Out]#     1 10.228702384362602  6.371498642092447 ... -7.3512096    -2.26345661680451
#[Out]#     2 16.725422758174165 10.517658588627857 ... -7.4015694  -2.5801735999000868
#[Out]#     3  3.667569668263566  18.64741107784339 ... -7.2589436 -0.05366344893150274
#[Out]#     4 10.893474047809809 19.225444192086155 ...  -7.331003   -0.763186396451086
#[Out]#     5 22.649774805175163 22.276100618544667 ...  -6.651447    -3.07849173923685
#[Out]#     6 20.299377890417478 21.582184693814497 ... -6.5624433  -2.6887658566938084
